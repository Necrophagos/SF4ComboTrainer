<Grid x:Class="FrameTrapped.ComboTrainer.Views.ComboTrainerView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:cal="clr-namespace:Caliburn.Micro;assembly=Caliburn.Micro"
      xmlns:controls="clr-namespace:FrameTrapped.ComboTrainer.Controls"
      mc:Ignorable="d" >

    <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*" MaxWidth="400"/>
        <ColumnDefinition  Width="Auto"/>
    </Grid.ColumnDefinitions>
    <Button Content="Load  SF4..." cal:Message.Attach="StartSF4">
        <Button.Style>
            <Style TargetType="Button"> 
                <Setter Property="FontSize" Value="40"/>
                <Setter Property="Foreground" Value="LightGray"/>
                <Style.Triggers>
                    <DataTrigger Binding="{Binding IsBusy}" Value="True">
                        <DataTrigger.Setters>
                            <Setter Property="Content" Value="Loading"/>
                        </DataTrigger.Setters>
                    </DataTrigger>
                </Style.Triggers>
            </Style>
        </Button.Style>
    </Button>
    <controls:WindowsHostHelper Grid.Column="1" Width="1280" Height="720" cal:Message.Attach="InitialiseWindowsHostHelper($source)" />
</Grid>
