<controls:MetroWindow x:Class="FrameTrapped.Views.AboutView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="http://metro.mahapps.com/winfx/xaml/controls"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        mc:Ignorable="d"
        d:DesignHeight="400"
        Width="410" 
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Title="{StaticResource Title}">
    <StackPanel Margin="10" >
        <StackPanel.Background>
            <ImageBrush ImageSource="../Resources/Images/AkumaAbout.png"/>
        </StackPanel.Background>
        <Separator />
        <TextBlock Text="{StaticResource AboutDescription}" Foreground="Black" FontSize="16" TextWrapping="WrapWithOverflow" HorizontalAlignment="Left" Width="140"/>
        <Separator Name="BottomSeparator" />
        <Button x:Name="TryClose" Content="OK" Width="80" Margin="3" />
    </StackPanel>
</controls:MetroWindow>
