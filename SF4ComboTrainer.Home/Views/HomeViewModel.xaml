<UserControl x:Class="SF4ComboTrainer.Home.Views.HomeViewModel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d">
    <UserControl.Resources>
        <Style x:Key="HomeViewButton" TargetType="{x:Type Button}">
            <Setter Property="Width" Value="128"/>
            <Setter Property="Height" Value="128"/>
        </Style>
    </UserControl.Resources>
    <Grid>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Button x:Name="ShowTimeLine" Style="{DynamicResource HomeViewButton}">
                <Image/>
            </Button>
            <Button x:Name="ShowLibrary" Style="{DynamicResource HomeViewButton}">
                <Image/>
            </Button>
            <Button Style="{DynamicResource HomeViewButton}">
                <Image/>
            </Button>
        </StackPanel>
    </Grid>
</UserControl>
